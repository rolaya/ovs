# KVM VNT network node configuration

#--------------------------------------------------------------------------------------
# KVM_VNT_GUEST_NAME: The name of the VNT network node.
unset KVM_VNT_GUEST_NAME
: ${KVM_VNT_GUEST_NAME:=kvm_node}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# KVM_VNT_HOST_RAM: The RAM (GB) of the KVM VNT VM.
unset KVM_VNT_GUEST_RAM
: ${KVM_VNT_GUEST_RAM:=1024}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# KVM_VNT_HOST_SIZE: The size (GB) of the KVM VNT VM.
unset KVM_VNT_GUEST_SIZE
: ${KVM_VNT_GUEST_SIZE:=5}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# KVM_VNT_GUEST_TYPE: The name of the VNT network node.
unset KVM_VNT_GUEST_TYPE
: ${KVM_VNT_GUEST_TYPE:="Linux"}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# KVM_VNT_GUEST_VARIANT: The name of the VNT network node.
unset KVM_VNT_GUEST_VARIANT
: ${KVM_VNT_GUEST_VARIANT:="debian9"}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# KVM_VNT_GUEST_ISO: The name of the VNT network node.
unset KVM_VNT_GUEST_ISO
: ${KVM_VNT_GUEST_ISO:="debian-9.12.0-amd64-DVD-1.iso"}
#--------------------------------------------------------------------------------------
