# Host specific network interface configuration

#######################################################################################
# Host (physical or virtual) network related configuration
#######################################################################################

#--------------------------------------------------------------------------------------
# HOST_NETIFACE_NAME: The physical (or virtual) network wired interface of the host.
# Note: this value could be gleaned/selected via "ip a"
# or some other means but for now it is hardcoded and needs
# to be updated according to environment where VNT is run.
unset HOST_NETIFACE_NAME
: ${HOST_NETIFACE_NAME:=eth0}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# HOST_NETIFACE_IP: The IP address (assigned by the DHCP server or statically 
# configured) of the VNT host's "physical" (wired) network interface. The VNT host
# can be a physical host or a VM (in test/development environments). The actual VNT
# host is a VM running on vSphere ESXi. Currently its IP address is: 10.181.4.100
unset HOST_NETIFACE_IP
: ${HOST_NETIFACE_IP:=192.168.1.57}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# GATEWAY_IP: The local network gatway IP address.
unset GATEWAY_IP
: ${GATEWAY_IP=192.168.1.1}
#--------------------------------------------------------------------------------------

