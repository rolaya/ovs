# CentOS provisioning and configuration

#--------------------------------------------------------------------------------------
# CENTOS_GROUP_GNOME_DESKTOP_ENV_ID: CentOS7 "GNOME Desktop Environment" group (ID).
# The list of groups can be obtained via "yum groups list".
# The ID of the "GNOME Desktop" group can be gleaned via "yum group info "GNOME Desktop"
unset CENTOS_GROUP_GNOME_DESKTOP_ENV_ID
: ${CENTOS_GROUP_GNOME_DESKTOP_ENV_ID:=gnome-desktop-environment}
#--------------------------------------------------------------------------------------
