# VNT environment provisioning and configuration

#######################################################################################
# VNT specific configuration
#######################################################################################

#--------------------------------------------------------------------------------------
# NUMBER_OF_VMS: Number of VMs in the VNT.
unset NUMBER_OF_VMS
: ${NUMBER_OF_VMS:=6}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# VM_BASE_NAME: VM base name. The VMs names can be anything, and right now are named 
# something like "vm-debian9-net-node1". The "VM_BASE_NAME" must be manually 
# changed here according to VNT configuration. It is assumed VMs are sequentially named).
unset VM_BASE_NAME
: ${VM_BASE_NAME="kvm-vnt-node"}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# VM_NAME_INDEX_BASE:
# 
unset VM_NAME_INDEX_BASE
: ${VM_NAME_INDEX_BASE:="1"}
#--------------------------------------------------------------------------------------

#######################################################################################
# Open vSwitch (OVS) and network related definitions
#######################################################################################

#--------------------------------------------------------------------------------------
# OVS_BRIDGE: The name of the OVS bridge to create (it can be anything)
unset OVS_BRIDGE
: ${OVS_BRIDGE:=br0}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# OVS_PORT_NAME_BASE: Default port name. Once generated, you will have something like:
# vnet1, vnet2, ... (depends on "NUMBER_OF_VMS").
unset OVS_PORT_NAME_BASE
: ${OVS_PORT_NAME_BASE:="vnet"}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# OVS_PORT_INDEX_BASE: Port index (base).
# 
unset OVS_PORT_INDEX_BASE
: ${OVS_PORT_INDEX_BASE:="0"}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# ADD_PORTS_TO_VSWITCH: Add ports to vSwitch.
unset ADD_PORTS_TO_VSWITCH
: ${ADD_PORTS_TO_VSWITCH:=false}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# ATTACH_VMS_TO_VSWITCH: Attach VMs to virtual switch flag.
unset ATTACH_VMS_TO_VSWITCH
: ${ATTACH_VMS_TO_VSWITCH:=false}
#--------------------------------------------------------------------------------------

#--------------------------------------------------------------------------------------
# AUTO_START_VMS: This flag should normally be set to true (because we want to auto 
# start the VMs in the VNT during deployment). If for testing purposes we do not want 
# to start the VMs immediately during deployment, set to false.
unset AUTO_START_VMS
: ${AUTO_START_VMS:=false}
#--------------------------------------------------------------------------------------




